<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="https://github.com/projektanker/icons.avalonia"
             x:Class="Sbroglione.Views.ComparisonView">

  <DockPanel>

    <!-- Header piatto -->
    <Border DockPanel.Dock="Top" Background="{DynamicResource Brush.Card}"
            BorderBrush="{DynamicResource Brush.CardBorder}" BorderThickness="0,0,0,1" Padding="20,14">
      <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
        <i:Icon Value="fa-solid fa-code-compare" FontSize="20" Foreground="{DynamicResource Brush.Accent}" />
        <TextBlock Text="{DynamicResource Str.Comparison.DirTitle}" FontSize="18" FontWeight="Bold"
                   Foreground="{DynamicResource Brush.TextPrimary}" VerticalAlignment="Center" />
      </StackPanel>
    </Border>

    <ScrollViewer Background="{DynamicResource Brush.Surface}">
      <StackPanel Margin="20" Spacing="14" MaxWidth="720" HorizontalAlignment="Left">

        <Border Classes="card">
          <StackPanel Spacing="10">
            <Grid ColumnDefinitions="Auto,*,Auto" RowDefinitions="Auto,8,Auto">
              <TextBlock Grid.Row="0" Grid.Column="0" Text="{DynamicResource Str.Comparison.Left}" Width="70" VerticalAlignment="Center"
                         Foreground="{DynamicResource Brush.TextPrimary}" />
              <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding LeftPath}" Watermark="{DynamicResource Str.Comparison.LeftWatermark}" />
              <Button Grid.Row="0" Grid.Column="2" Classes="iconbtn" Command="{Binding BrowseLeftCommand}"
                      ToolTip.Tip="{DynamicResource Str.Common.Browse}">
                <i:Icon Value="fa-solid fa-folder-open" />
              </Button>

              <TextBlock Grid.Row="2" Grid.Column="0" Text="{DynamicResource Str.Comparison.Right}" Width="70" VerticalAlignment="Center"
                         Foreground="{DynamicResource Brush.TextPrimary}" />
              <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding RightPath}" Watermark="{DynamicResource Str.Comparison.RightWatermark}" />
              <Button Grid.Row="2" Grid.Column="2" Classes="iconbtn" Command="{Binding BrowseRightCommand}"
                      ToolTip.Tip="{DynamicResource Str.Common.Browse}">
                <i:Icon Value="fa-solid fa-folder-open" />
              </Button>
            </Grid>

            <StackPanel Orientation="Horizontal" Spacing="8">
              <Button Classes="primary" Command="{Binding CompareCommand}" IsEnabled="{Binding !IsComparing}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <i:Icon Value="fa-solid fa-code-compare" />
                  <TextBlock Text="{DynamicResource Str.Comparison.Compare}" />
                </StackPanel>
              </Button>
              <Button Classes="secondary" Command="{Binding CancelCommand}" IsEnabled="{Binding IsComparing}">
                <TextBlock Text="{DynamicResource Str.Common.Cancel}" />
              </Button>
            </StackPanel>

            <TextBlock Text="{Binding StatusText}" Foreground="{DynamicResource Brush.TextMuted}" />
          </StackPanel>
        </Border>

        <Border Classes="card" IsVisible="{Binding HasResult}">
          <StackPanel Spacing="10">
            <TextBlock Text="{DynamicResource Str.Comparison.Result}" FontSize="15" FontWeight="SemiBold"
                       Foreground="{DynamicResource Brush.TextPrimary}" />

            <UniformGrid Columns="4">
              <StackPanel Spacing="2">
                <TextBlock Text="{Binding IdenticalCount}" FontSize="20" FontWeight="Bold"
                           Foreground="{DynamicResource Brush.TextPrimary}" />
                <TextBlock Text="{DynamicResource Str.Comparison.Identical}" Foreground="{DynamicResource Brush.TextMuted}" FontSize="12" />
              </StackPanel>
              <StackPanel Spacing="2">
                <TextBlock Text="{Binding DifferentCount}" FontSize="20" FontWeight="Bold"
                           Foreground="{DynamicResource Brush.TextPrimary}" />
                <TextBlock Text="{DynamicResource Str.Comparison.Different}" Foreground="{DynamicResource Brush.TextMuted}" FontSize="12" />
              </StackPanel>
              <StackPanel Spacing="2">
                <TextBlock Text="{Binding LeftOnlyCount}" FontSize="20" FontWeight="Bold"
                           Foreground="{DynamicResource Brush.TextPrimary}" />
                <TextBlock Text="{DynamicResource Str.Comparison.LeftOnly}" Foreground="{DynamicResource Brush.TextMuted}" FontSize="12" />
              </StackPanel>
              <StackPanel Spacing="2">
                <TextBlock Text="{Binding RightOnlyCount}" FontSize="20" FontWeight="Bold"
                           Foreground="{DynamicResource Brush.TextPrimary}" />
                <TextBlock Text="{DynamicResource Str.Comparison.RightOnly}" Foreground="{DynamicResource Brush.TextMuted}" FontSize="12" />
              </StackPanel>
            </UniformGrid>

            <StackPanel Orientation="Horizontal" Spacing="8">
              <TextBlock Text="{DynamicResource Str.Comparison.ExportLabel}" VerticalAlignment="Center"
                         Foreground="{DynamicResource Brush.TextPrimary}" />
              <Button Classes="secondary" Command="{Binding ExportHtmlCommand}"><TextBlock Text="HTML" /></Button>
              <Button Classes="secondary" Command="{Binding ExportCsvCommand}"><TextBlock Text="CSV" /></Button>
              <Button Classes="secondary" Command="{Binding ExportJsonCommand}"><TextBlock Text="JSON" /></Button>
            </StackPanel>
          </StackPanel>
        </Border>

        <Border Classes="card">
          <StackPanel Spacing="10">
            <TextBlock Text="{DynamicResource Str.Comparison.FileTitle}" FontSize="15" FontWeight="SemiBold"
                       Foreground="{DynamicResource Brush.TextPrimary}" />

            <Grid ColumnDefinitions="Auto,*,Auto" RowDefinitions="Auto,8,Auto">
              <TextBlock Grid.Row="0" Grid.Column="0" Text="{DynamicResource Str.Comparison.File1}" Width="70" VerticalAlignment="Center"
                         Foreground="{DynamicResource Brush.TextPrimary}" />
              <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding LeftFilePath}" Watermark="{DynamicResource Str.Comparison.File1Watermark}" />
              <Button Grid.Row="0" Grid.Column="2" Classes="iconbtn" Command="{Binding BrowseLeftFileCommand}"
                      ToolTip.Tip="{DynamicResource Str.Common.Browse}">
                <i:Icon Value="fa-solid fa-file" />
              </Button>

              <TextBlock Grid.Row="2" Grid.Column="0" Text="{DynamicResource Str.Comparison.File2}" Width="70" VerticalAlignment="Center"
                         Foreground="{DynamicResource Brush.TextPrimary}" />
              <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding RightFilePath}" Watermark="{DynamicResource Str.Comparison.File2Watermark}" />
              <Button Grid.Row="2" Grid.Column="2" Classes="iconbtn" Command="{Binding BrowseRightFileCommand}"
                      ToolTip.Tip="{DynamicResource Str.Common.Browse}">
                <i:Icon Value="fa-solid fa-file" />
              </Button>
            </Grid>

            <StackPanel Orientation="Horizontal" Spacing="8">
              <Button Classes="primary" Command="{Binding CompareFilesCommand}" IsEnabled="{Binding !IsFileComparing}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <i:Icon Value="fa-solid fa-code-compare" />
                  <TextBlock Text="{DynamicResource Str.Comparison.CompareFiles}" />
                </StackPanel>
              </Button>
              <Button Classes="secondary" Command="{Binding CancelFileCompareCommand}"
                      IsEnabled="{Binding IsFileComparing}">
                <TextBlock Text="{DynamicResource Str.Common.Cancel}" />
              </Button>
            </StackPanel>

            <TextBlock Text="{Binding FileCompareStatus}" Foreground="{DynamicResource Brush.TextMuted}" />

            <StackPanel Spacing="4" IsVisible="{Binding HasFileResult}">
              <TextBlock Text="{Binding LengthsText}" Foreground="{DynamicResource Brush.TextMuted}" />
              <TextBlock Text="{Binding FirstDiffText}" Foreground="{DynamicResource Brush.TextPrimary}" />
              <TextBlock Text="{Binding IdenticalPercentText}" Foreground="{DynamicResource Brush.TextPrimary}" />
              <TextBlock Text="{Binding RangeCountText}" Foreground="{DynamicResource Brush.TextMuted}" />
            </StackPanel>
          </StackPanel>
        </Border>

      </StackPanel>
    </ScrollViewer>

  </DockPanel>

</UserControl>
